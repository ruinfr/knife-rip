services:
  db:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: arivix
    ports:
      - "5432:5432"
    volumes:
      - arivix_pg:/var/lib/postgresql/data

volumes:
  arivix_pg:
